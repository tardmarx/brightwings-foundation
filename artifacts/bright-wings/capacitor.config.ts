import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.brightwings.foundation',
  appName: 'Bright Wings Foundation',
  webDir: 'dist/public',
};

export default config;
